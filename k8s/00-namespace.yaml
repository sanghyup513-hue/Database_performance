# 부하 생성·자동화 도구(BenchBase 등)를 위한 전용 네임스페이스.
# DB는 클러스터 밖 외부 서버에 위치하므로, 이 네임스페이스에는
# 부하 생성기와 워크로드 실행용 Job/CronJob 만 배치한다.
apiVersion: v1
kind: Namespace
metadata:
  name: benchmark
  labels:
    app.kubernetes.io/part-of: db-perf-benchmark
    purpose: load-generation
